# Batch Connect - RStudio Server

An Open OnDemand Batch Connect app that launches RStudio Server as an interactive session on the Mantis cluster.

Uses a rocker project container https://rocker-project.org

Fetched with `apptainer pull docker:rocker/rstudio:<version>